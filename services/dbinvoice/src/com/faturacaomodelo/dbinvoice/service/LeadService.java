/*Generated by WaveMaker Studio*/
package com.faturacaomodelo.dbinvoice.service;

/*This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/

import java.util.Map;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wavemaker.runtime.data.exception.EntityNotFoundException;
import com.wavemaker.runtime.data.export.ExportType;
import com.wavemaker.runtime.data.expression.QueryFilter;
import com.wavemaker.runtime.data.model.AggregationInfo;
import com.wavemaker.runtime.file.model.Downloadable;

import com.faturacaomodelo.dbinvoice.Lead;
import com.faturacaomodelo.dbinvoice.LeadContactRel;

/**
 * Service object for domain model class {@link Lead}.
 */
public interface LeadService {

    /**
     * Creates a new Lead. It does cascade insert for all the children in a single transaction.
     *
     * This method overrides the input field values using Server side or database managed properties defined on Lead if any.
     *
     * @param lead Details of the Lead to be created; value cannot be null.
     * @return The newly created Lead.
     */
	Lead create(Lead lead);


	/**
	 * Returns Lead by given id if exists.
	 *
	 * @param leadId The id of the Lead to get; value cannot be null.
	 * @return Lead associated with the given leadId.
     * @throws EntityNotFoundException If no Lead is found.
	 */
	Lead getById(Integer leadId) throws EntityNotFoundException;

    /**
	 * Find and return the Lead by given id if exists, returns null otherwise.
	 *
	 * @param leadId The id of the Lead to get; value cannot be null.
	 * @return Lead associated with the given leadId.
	 */
	Lead findById(Integer leadId);


	/**
	 * Updates the details of an existing Lead. It replaces all fields of the existing Lead with the given lead.
	 *
     * This method overrides the input field values using Server side or database managed properties defined on Lead if any.
     *
	 * @param lead The details of the Lead to be updated; value cannot be null.
	 * @return The updated Lead.
	 * @throws EntityNotFoundException if no Lead is found with given input.
	 */
	Lead update(Lead lead) throws EntityNotFoundException;

    /**
	 * Deletes an existing Lead with the given id.
	 *
	 * @param leadId The id of the Lead to be deleted; value cannot be null.
	 * @return The deleted Lead.
	 * @throws EntityNotFoundException if no Lead found with the given id.
	 */
	Lead delete(Integer leadId) throws EntityNotFoundException;

	/**
	 * Find all Leads matching the given QueryFilter(s).
     * All the QueryFilter(s) are ANDed to filter the results.
     * This method returns Paginated results.
	 *
     * @deprecated Use {@link #findAll(String, Pageable)} instead.
	 *
     * @param queryFilters Array of queryFilters to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching Leads.
     *
     * @see QueryFilter
     * @see Pageable
     * @see Page
	 */
    @Deprecated
	Page<Lead> findAll(QueryFilter[] queryFilters, Pageable pageable);

    /**
	 * Find all Leads matching the given input query. This method returns Paginated results.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching Leads.
     *
     * @see Pageable
     * @see Page
	 */
    Page<Lead> findAll(String query, Pageable pageable);

    /**
	 * Exports all Leads matching the given input query to the given exportType format.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param exportType The format in which to export the data; value cannot be null.
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return The Downloadable file in given export type.
     *
     * @see Pageable
     * @see ExportType
     * @see Downloadable
	 */
    Downloadable export(ExportType exportType, String query, Pageable pageable);

	/**
	 * Retrieve the count of the Leads in the repository with matching query.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query query to filter results. No filters applied if the input is null/empty.
	 * @return The count of the Lead.
	 */
	long count(String query);

	/**
	 * Retrieve aggregated values with matching aggregation info.
     *
     * @param aggregationInfo info related to aggregations.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
	 * @return Paginated data with included fields.

     * @see AggregationInfo
     * @see Pageable
     * @see Page
	 */
	Page<Map<String, Object>> getAggregatedValues(AggregationInfo aggregationInfo, Pageable pageable);

    /*
     * Returns the associated leadContactRels for given Lead id.
     *
     * @param id value of id; value cannot be null
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of associated LeadContactRel instances.
     *
     * @see Pageable
     * @see Page
     */
    Page<LeadContactRel> findAssociatedLeadContactRels(Integer id, Pageable pageable);

}

